language: scala
sudo: true
services:
  - mysql
  - postgresql
  - docker
before_install:
#  - docker pull mysql
#  - docker run --name mysql -d -p 127.0.0.1:3306:3306 -e MYSQL_ROOT_PASSWORD=root mysql
#  - docker inspect mysql
  - docker pull wnameless/oracle-xe-11g
  - docker pull microsoft/mssql-server-linux
  - docker run --name oracle -d -p 127.0.0.1:1521:1521 -e ORACLE_ALLOW_REMOTE=true wnameless/oracle-xe-11g
  - docker run --name sqlserver -d -p 127.0.0.1:1433:1433 -e DBCA_TOTAL_MEMORY=1024 -e ACCEPT_EULA=Y -e SA_PASSWORD=P4ssw0rd microsoft/mssql-server-linux
  - docker inspect oracle
  - docker inspect sqlserver
  - docker ps -a
scala:
  - 2.11.8
jdk:
  - oraclejdk8
before_script:
  - psql -c 'create database docker;' -U postgres
addons:
  postgresql: "9.4"
script: "./build.sh"
